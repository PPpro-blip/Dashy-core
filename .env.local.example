# DashyCore v7 — Next.js frontend environment
# Copy to .env.local and fill in real values.

# Supabase (used for Supabase Auth session on the client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Dashy Flow-State worker (real chat/model/RAG endpoint)
# URL of the deployed Cloudflare Worker
NEXT_PUBLIC_DASHY_FLOW_STATE_URL=https://dashy-flow-state.your-subdomain.workers.dev

# Dashy Digest worker (document/screenshot ingestion endpoint).
# Browser code NEVER calls this directly any more: uploads go to the
# same-origin /api/digest/upload proxy, which injects the Supabase userId.
NEXT_PUBLIC_DASHY_DIGEST_URL=https://dashy-digest.your-subdomain.workers.dev

# Server-side override for the digest worker used by /api/digest/upload.
# Takes precedence over NEXT_PUBLIC_DASHY_DIGEST_URL on the server.
DASHY_DIGEST_URL=https://dashy-digest.your-subdomain.workers.dev

# CORS origin allowed by the worker (must match the worker's CORS_ORIGIN)
# Leave at http://localhost:3000 for local development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ElevenLabs realistic voice engine (/api/voice/elevenlabs). Server-only —
# never prefix with NEXT_PUBLIC_. Users may also paste their own key in
# Settings → Voice (kept in their browser, relayed per request).
ELEVENLABS_API_KEY=
# Optional defaults (voice from ElevenLabs → Voices; model: eleven_multilingual_v2
# for the most natural delivery, eleven_flash_v2_5 for the lowest latency).
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_MODEL_ID=eleven_multilingual_v2

# Knowledge Digest quota shown by the /analytics memory gauge (documents).
NEXT_PUBLIC_DIGEST_QUOTA_DOCS=100
